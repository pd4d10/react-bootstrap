import { Playground, PropsTable } from 'docz'
import { ButtonGroup, ButtonToolbar, Button } from '../lib'

## Example

<Playground>
  <ButtonGroup>
    <Button theme="secondary">Left</Button>
    <Button theme="secondary">Middle</Button>
    <Button theme="secondary">Right</Button>
  </ButtonGroup>
</Playground>

<Playground>
  <ButtonToolbar>
    <ButtonGroup className="mr-2">
      <Button theme="secondary">1</Button>
      <Button theme="secondary">2</Button>
      <Button theme="secondary">3</Button>
      <Button theme="secondary">4</Button>
    </ButtonGroup>
    <ButtonGroup className="mr-2">
      <Button theme="secondary">5</Button>
      <Button theme="secondary">6</Button>
      <Button theme="secondary">7</Button>
    </ButtonGroup>
    <ButtonGroup>
      <Button theme="secondary">8</Button>
    </ButtonGroup>
  </ButtonToolbar>
</Playground>

<Playground>
  <ButtonGroup>
    <Button theme="secondary" size="lg">
      Large
    </Button>
  </ButtonGroup>
</Playground>

## Sizing

<Playground>
  {['lg', undefined, 'sm'].map(size => (
    <div key={size} className="mt-1 mb-1">
      <ButtonGroup>
        {['Left', 'Middle', 'Right'].map(text => (
          <Button size={size} theme="secondary">
            {text}
          </Button>
        ))}
      </ButtonGroup>
    </div>
  ))}
</Playground>
