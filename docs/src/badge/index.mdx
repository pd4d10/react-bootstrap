import { DemoContainer } from '../example-container'

## Example

Badges scale to match the size of the immediate parent element.

<DemoContainer
  component={require('./example').Example}
  code={require('!!raw-loader!./example.js')}
/>

Badges can be used as part of links or buttons to provide a counter.

<DemoContainer
  component={require('./counter').Example}
  code={require('!!raw-loader!./counter.js')}
/>

## Contextual variations

Specify `theme` to change the appearance of a badge.

<DemoContainer
  component={require('./theme').Example}
  code={require('!!raw-loader!./theme.js')}
/>

## Pill badges

Add `pill` to make badges more rounded.

<DemoContainer
  component={require('./pill').Example}
  code={require('!!raw-loader!./pill.js')}
/>

## Links

<DemoContainer
  component={require('./link').Example}
  code={require('!!raw-loader!./link.js')}
/>
