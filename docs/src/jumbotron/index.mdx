import { DemoContainer } from '../example-container'

## Example

<DemoContainer
  component={require('./jumbotron').Example}
  code={require('!!raw-loader!./jumbotron')}
/>

<DemoContainer
  component={require('./fluid').Example}
  code={require('!!raw-loader!./fluid')}
/>
