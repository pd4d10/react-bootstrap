import { DemoContainer } from '../example-container'

## Examples

Specify `theme` to use predefined button styles.

<DemoContainer
  component={require('./theme').Example}
  code={require('!!raw-loader!./theme')}
/>

## Outline buttons

Add `outline` to remove all background images and colors.

<DemoContainer
  component={require('./outline').Example}
  code={require('!!raw-loader!./outline')}
/>

## Sizes

Specify `size` for additional sizes.

<DemoContainer
  component={require('./size').Example}
  code={require('!!raw-loader!./size')}
/>

## Active state

Add `active` to show active appearance

<DemoContainer
  component={require('./active').Example}
  code={require('!!raw-loader!./active')}
/>

## Disabled state

Add `disabled` to make buttons look inactive

<DemoContainer
  component={require('./disabled').Example}
  code={require('!!raw-loader!./disabled')}
/>
